version: 2.1

orbs:
  cypress: cypress-io/cypress@1.1.0

jobs:
  test:
    docker:
      - image: cypress/base:10
    steps:
      - checkout
      - run: npm ci
      - run: npm test

workflows:
  build:
    jobs:
      - test
      - cypress/run:
          requires:
            - test
          start: 'npm start'
          wait-on: 'http://localhost:8000'
